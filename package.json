{
  "name": "jungle-challenge",
  "private": true,
  "packageManager": "pnpm@8.13.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
